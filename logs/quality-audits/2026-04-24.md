# Quality Audit Log — 2026-04-24

First regular checkup per `99_scitex-quality-checklist.md` §10b.
Triggered manually via `/speak-and-call` (on-demand, not waiting for
nightly cron).

## 01:00 UTC — Ecosystem CI dependency sweep

**Root cause:** ~14 packages failing nightly CI due to missing
`scitex-dev` test dep in CI install step.

**Fixes applied (8 pkgs):**
- scitex-browser: `pip install scitex-dev` + `pytest.importorskip("scitex_dev")` (commits 50c7c7d, 234eaae)
- scitex-path: `pip install pytest pytest-cov scitex-dev numpy` + `importorskip("git")` + deep-fix `test_get_data_path_success` (ca39f82, fe4c62d, 707650b)
- scitex-repro: `scitex-dev` added (ff01148)
- scitex-types: `scitex-dev` + `_torch` sentinel + `importorskip("torch")` (53839af, 628ab1e, 68e1c43)
- scitex-audit, scitex-core, scitex-gists: develop-branch create + `scitex-dev` added (cross-commits)
- scitex-io: plotly + Pillow → `[dev]` extras, pandas `str` dtype guard (4313f7b, 636ac6a)
- scitex-core deep-fix: logging/`SUCCESS` + `Tee` + `repro.hash_array` + `sh` + `types.is_listed_X` signatures + `_has_torch` module-level (665330f, a620a11, + deep-fix commits)
- scitex-gists: line-anchored `^Function` regex (23ae465)

**Branch anomalies resolved:**
- scitex-agent-container: feature branch → develop fast-forwarded, pushed (0386fd05 downstream)
- scitex-ui: feature branch merged → develop, feature branch deleted
- scitex-audit, scitex-core, scitex-gists: develop branch created from main

**Push state cleared:**
- scitex-notebook +1 pushed (f7a206a)
- scitex-notification +3 pushed (bdb b788, 2593b58, e202b96)

**Doc-Drift Nightly (scitex-python):**
- Root cause trace: 10-min timeout → bumped to 25 min (c0af5136) → still failed with `ImportError: formatargspec` on ancient wrapt → added `pip install wrapt>=1.15` (b84b44b7) → transitive resolver still picked old wrapt → `PIP_CONSTRAINT=wrapt>=1.17` (6c13ba4a, in-flight as run 24868741284).

## Outstanding (flagged for user)

- scitex-clew: `tests/test__api.py::test_all_count` asserts 19 `__all__` exports, now 20 — 1-line test update needed. In user's dirty tree.
- scitex-core: tests pass but 22 tests were genuinely buggy; real bugs fixed, no residual debt.
- 17 PyPI wheels need re-release to surface recent `_skills/` content (delegated to subagent in this pass).
- 5 READMEs lack Problem/Solution table: scitex-agent-container, scitex-notebook, scitex-notification, scitex-orochi, scitex-plt (delegated to subagent).
- scitex-doc: empty local / 404 remote — needs setup or removal.
- Doc-Drift Nightly waiting on run 24868741284 result.

## Checklist additions / lessons (folded back into §3 table)

- `wrapt<1.15` + Py3.11 → formatargspec removed → pin via `PIP_CONSTRAINT`
- pip resolver backtracking on sphinx stack → raise `timeout-minutes`
- feature branch on develop-convention repo → advance via `git update-ref` to avoid touching user's dirty tree

## Metrics

- Packages audited: 44 (of 48 scitex-* dirs; 4 non-library)
- Packages with CI now green: ~34 of 34 targeted (passing) + 3 no-runs
- Branch convention compliance: 44/44 on develop (or develop-created)
- Push state clean: 44/44
- SKILL.md frontmatter complete: 33/33
- P/S table: 27/32 → 32/32 after this pass (pending subagent)

## Next scheduled check

- Next automated cron: 07:00 UTC nightly (per-package) + 07:15 agentic + 07:30 doc-drift (all tomorrow).

## Second-half of pass (15% polish)

**P/S tables added to 2/5 target packages** (commits 82f5f90, 29c07f2):
- scitex-notification: 3-row P/S + interface callout
- scitex-plt: 3-row P/S + interface callout
- 3 skipped (dirty trees): scitex-agent-container, scitex-notebook, scitex-orochi

**PyPI releases landed: 11/12** (via trusted publishing):
- scitex-audit 0.1.2, scitex-browser 0.1.8, scitex-core 0.2.2, scitex-dataset 0.3.2, scitex-dict 0.1.2, scitex-gists 0.1.2, scitex-linter 0.3.5, scitex-logging 0.1.2, scitex-path 0.1.2, scitex-repro 0.1.2, scitex-types 0.1.2
- scitex-plt v0.24.2: `invalid-publisher` error (§11 of checklist) — trusted publisher not configured on PyPI web form; requires manual fix

**scitex-plt workflow bootstrap** (commit 5b398f6): added `publish-pypi.yml` + `test.yml` by copying from scitex-core. scitex-plt now has nightly CI infra; just needs PyPI trusted-publisher form entry.

**Doc-Drift Nightly — 5-layer fix to first GREEN** (scitex-python):
1. `c0af5136`: bumped timeout-minutes 10 → 25
2. `b84b44b7`: `pip install wrapt>=1.15` (insufficient)
3. `6c13ba4a`: `PIP_CONSTRAINT=wrapt>=1.17` env var (resolver backtracked anyway)
4. `66a53dee`: switched `pip install` → `uv pip install` (install now ~2min not 25+)
5. `ecd3f149`: auditor `hasattr` catches ImportError from umbrella `__getattr__`
6. `dde6c1e7`: `scitex.scholar` shim guards `clean_abstract` for PyPI 1.2.x compat
7. `9d110b49`: clone downstream repos from `develop` branch (not default `main`)
8. `72bb67aa`: centralize audit scripts to scitex-dev + doc references

**Final Doc-Drift status: GREEN** (run 24869929833, 2026-04-24 02:57 UTC).

## Maintenance scripts centralization

Moved to `scitex-dev/scripts/quality/` (uncommitted — dirty tree):
- `audit_doc_examples.py`
- `audit_test_scope.py`
- `audit_line_limits.py`
- `line_limits_allowlist.txt`
- `_cli_quality.py` (`python -m scitex_dev._cli_quality audit_{docs,scope,lines}`)

Mirrored in scitex-python for now. Workflow still calls scitex-python/scripts/ — will switch after scitex-dev release lands.

## Checklist doc updates

`99_scitex-quality-checklist.md` §4, §7, §16 now reference
`scitex-dev/scripts/quality/` as canonical with scitex-python mirror note.

## Remaining (user-assignable)

1. **scitex-plt trusted publisher**: configure at pypi.org/manage/project/scitex-plt/settings/publishing/ (owner ywatanabe1989, repo scitex-plt, workflow publish-pypi.yml, env pypi), then `gh run rerun 24869380645`.
2. **scitex-clew `__all__` count**: 1-line test update when ready to commit scitex-clew dirty tree.
3. **3 missing P/S tables** (scitex-agent-container, scitex-notebook, scitex-orochi): after dirty trees are committed.
4. **scitex-dev commits**: user decides when to commit uncommitted files including new `scripts/quality/` and `_cli_quality.py`.
5. **scitex-doc**: empty local / 404 remote — setup or delete.
6. **scitex-scholar PyPI re-release to 1.3.x** (once `clean_abstract` lands in release) would let the umbrella shim drop the fallback.

---

<!-- EOF -->
